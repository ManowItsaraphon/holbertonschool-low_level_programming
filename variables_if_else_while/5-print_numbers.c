#include <stdio.h>

/**
 * main - print single digit of number
 *
 * Return: Always 0
 *
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
